# trydocpie #

## Start ##

```bash
yarn install
yarn serverinstall
```

*   start: `yarn start`
*   build: `yarn build`
*   lint fix: `./lint.sh` and `./lint.sh --fix`
*   release: `./release.sh <host>`
